#include <bits/stdc++.h>
using namespace std;

int main() {
    freopen("out.txt", "w", stdout);
    const int N = 1e5;
    cout << N << ' ' << 0 << endl;
    for (int i = 0; i < N; ++i) {
        cout << 1 << ' ';
    }
    cout << endl;
}
