#include <bits/stdc++.h>
using namespace std;

#if __APPLE__
#define eprintf(...) fprintf(stderr, __VA_ARGS__)
#else
#define eprintf(...)
#endif

int main() {
#if __APPLE__
    freopen(".in", "r", stdin);
    freopen(".out", "w", stdout);
#endif
}
