#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;

int main(int argc, char* argv[]) {
	registerGen(argc, argv, 1);

	int n = atoi(argv[1]);
	int h = atoi(argv[2]);

	cout << n << " " << h << "\n";

	return 0;
}
